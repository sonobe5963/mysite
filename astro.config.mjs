// @ts-check
import { defineConfig } from 'astro/config';

// // https://astro.build/config
// export default defineConfig({});

export default defineConfig({
  // あなたのGitHub PagesのURL（予定）
  site: 'https://sonobe5963.github.io',
  // リポジトリ名が mysite なら、ここを '/mysite' にする
  base: '/mysite',
});